# Changelog

## v0.6.0

- Removed `model_core.infrastructure.legacy`.
- Use `model_core.infrastructure.adapters` for trainer compatibility imports.
- CLI entrypoints remain unchanged (`run_cn_train.py`, `run_cn_backtest.py`).
